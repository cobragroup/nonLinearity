# Non Linearity in mutual information

Launch the program calling cli.py, it allows to specify a different config file and to override dataset and number of bins.

The access information for the dataset (at least the path to the file), must be in the config file.

`.mat` files are expected to contain data as time_samples x brain_region x session.